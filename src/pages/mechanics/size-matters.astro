---
export const prerender = true;
import MechanicPage from "../../components/gamebob/mechanics/MechanicPage.astro";
import MechanicCanvas from "../../components/gamebob/mechanics/MechanicCanvas.astro";
import MechanicInfo from "../../components/gamebob/mechanics/MechanicInfo.astro";
import { Icon } from "astro-icon/components";

const GITHUB_BASE = "https://github.com/Game-Bob/website/tree/main/src/mechanics/size-matters";
const CODE_URL =
    "https://github.com/Game-Bob/website/blob/main/src/mechanics/size-matters/SizeMattersGame.js";
---

<MechanicPage
    title="Size Matters"
    description="El tamaño importa. Crece para romper, encoge para flotar."
    githubUrl={GITHUB_BASE}
    codeurl={CODE_URL}

    image="/images/gamebob/mechanics/size-matters.webp"
>
    <Icon slot="icon" name="mdi:arrow-expand-all" style="color: var(--color-accent)" />

    <MechanicCanvas
        id="game-canvas"
        instruction="Mantén para Crecer"
        subInstruction="Suelta para Encoger"
        verticalOnMobile={true}

    />

    <MechanicInfo>
        <Fragment slot="how-it-works">
            <li>
                <span style="color: var(--color-primary)">•</span>
                <span
                    >Mantén pulsado para <strong>CRECER</strong>: rompes bloques rojos, pero caes
                    rápido.</span
                >
            </li>
            <li>
                <span style="color: var(--color-primary)">•</span>
                <span>Suelta para <strong>ENCOGER</strong>: flotas y pasas por huecos azules.</span>
            </li>
            <li>
                <span style="color: var(--color-primary)">•</span>
                <span>Física de masa variable y gravedad dinámica.</span>
            </li>
        </Fragment>

        <Fragment slot="controls">
            <div class="control-item">
                <span class="control-key">Click / Tap (Mantener)</span>
                <span class="control-desc">Crecer / Aumentar Masa</span>
            </div>
        </Fragment>
    </MechanicInfo>
</MechanicPage>

<script>
    import { SizeMattersGame } from "../../../mechanics/size-matters/SizeMattersGame";

    const canvas = document.getElementById("game-canvas") as HTMLCanvasElement;
    const overlay = document.getElementById("game-canvas-overlay");
    let game: game | null = null;

    if (canvas && overlay) {
        const rect = canvas.getBoundingClientRect();
        canvas.width = rect.width;
        canvas.height = rect.height;

        overlay.addEventListener("click", () => {
            overlay.style.opacity = "0";
            overlay.style.pointerEvents = "none";
            if (!game) {
                game = new SizeMattersGame(canvas);
            }
        });

        window.addEventListener("resize", () => {
            if (game) game.resize();
        });
    }
</script>
