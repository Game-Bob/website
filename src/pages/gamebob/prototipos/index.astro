---
export const prerender = true;
import "./index.css";
import GameBobLayout from "../../../layouts/GameBobLayout.astro";
import { Icon } from "astro-icon/components";
import { prototypes } from "../../../data/gamebob/prototypes";

---

<GameBobLayout
    title="Prototipos - GameBob"
    description="Zona de pruebas experimental. Prototipos en fase muy temprana (pre-alpha) para validar ideas de juego."
    image="/images/gamebob/prototipos/index.webp"
>
    <div class="prototypes-page">
        <div class="prototypes-header">
            <h1 class="prototypes-title">
                prototipos
            </h1>
            <p class="prototypes-subtitle">
                zona de experimentación radical. ideas en fase muy temprana.
            </p>
        </div>

        <div class="warning-box">
            <Icon name="mdi:alert-decagram" class="warning-icon" />
            <div class="warning-content">
                <h3 class="warning-title">
                    Advertencia: Zona Inestable
                </h3>
                <p class="warning-text">
                    Estos prototipos están tan verdes que todavía hacen la fotosíntesis. Son
                    versiones muy tempranas creadas únicamente para validar si una idea rudimentaria
                    es divertida. Pueden contener errores, romperse sin previo aviso o no estar
                    optimizados. Juega bajo tu propia responsabilidad. Y ni se te ocurre divertirte,
                    esto es trabajo puro y duro.
                </p>
            </div>
        </div>

        <div class="prototypes-grid">
            {
                prototypes.map((proto) => (
                    <a
                        href={`/gamebob/prototipos/${proto.slug}/`}
                        class="prototype-card"
                        style={`border-color: ${proto.themeColor}33;`}
                        onmouseover={`this.style.backgroundColor='${proto.themeColor}1a'`}
                        onmouseout="this.style.backgroundColor='rgba(15, 23, 42, 0.5)'"
                    >
                        <div class="card-arrow">
                            <Icon name="mdi:arrow-top-right" class="arrow-icon-sm" />
                        </div>

                        <div class="proto-icon-container">
                            <Icon name={proto.icon} class="proto-icon" style={`color: ${proto.themeColor}`} />
                        </div>

                        <h2 class="proto-title">
                            {proto.title}
                        </h2>
                        <p class="proto-description">
                            {proto.description}
                        </p>

                        {proto.verdict === "liked" && (
                            <div class="verdict-badge verdict-liked">
                                <Icon name="mdi:thumb-up" class="footer-icon" />
                                Ma gustao
                            </div>
                        )}

                        {proto.verdict === "disliked" && (
                            <div class="verdict-badge verdict-disliked">
                                <Icon name="mdi:thumb-down" class="footer-icon" />
                                No ma gustao
                            </div>
                        )}
                    </a>
                ))
            }
        </div>

        <div class="proto-legend">
            <h3 class="legend-title">
                Leyenda de Veredictos
            </h3>
            <div class="legend-grid">
                <div class="legend-item liked">
                    <Icon name="mdi:thumb-up" class="legend-icon liked" />
                    <div class="legend-content">
                        <div class="legend-label verdict-liked">
                            Ma gustao
                        </div>
                        <p class="legend-text">
                            Posiblemente en algún momento se vuelva un juego más completo dándole
                            una vuelta a las mecánicas y puliéndolo.
                        </p>
                    </div>
                </div>

                <div class="legend-item disliked">
                    <Icon name="mdi:thumb-down" class="legend-icon disliked" />
                    <div class="legend-content">
                        <div class="legend-label verdict-disliked">
                            No ma gustao
                        </div>
                        <p class="legend-text">
                            La idea resonaba increíble en mi cabeza pero no ha aterrizado. Si sabes
                            cómo arreglarlo, contacta conmigo para ver si salvamos este
                            despropósito.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</GameBobLayout><GameBobLayouttitle=description=image=<divclass=<divclass=<h1class=prototipos</h1><pclass=zonadeexperimentaciónradical.ideasenfasemuytemprana.</p></div><divclass=<Iconname=class=<divclass=<h3class=Advertencia